<div class='page-header'>
  {{#linkTo 'projects.edit' content class='btn btn-small pull-right'}} Edit {{/linkTo}}
  <h2>{{ title }}</h2>
</div>
<div class='row'>
  <ul class="stacks">
  {{#each controllers.stacks}}
    {{partial 'stacks/stack'}}
  {{/each}}
  </ul>
</div>
